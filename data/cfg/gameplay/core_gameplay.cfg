locale it {
	text impact { text = impatto; };
	text pierce { text = perfora; };
	text poison { text = veleno; };
	text fire { text = fuoco; };
	
	text poisoned { text = avvelenato; };
};

locale en {
	text impact { text = impact; };
	text pierce { text = pierce; };
	text poison { text = poison; };
	text fire { text = fire; };
	
	text poisoned { text = poisoned; };
};

damageType impact {
	description = impact damage;
	type = physical;
};

damageType pierce {
	description = pierce damage;
	type = physical;
};

damageType poison {
	description = poison damage;
	type = magical;
};

damageType fire {
	description = fire damage;
	type = magical;
};